import 'package:flutter/material.dart';

class ProductsScreenOverview extends StatelessWidget {
  const ProductsScreenOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
